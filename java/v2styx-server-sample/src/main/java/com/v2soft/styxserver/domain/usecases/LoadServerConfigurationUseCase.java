package com.v2soft.styxserver.domain.usecases;

import com.v2soft.styxserver.domain.ServerConfiguration;

public class LoadServerConfigurationUseCase(
        ObjectMapper mapper
        ) {
    ServerConfiguration load(String configurationFile) {

    }
}
