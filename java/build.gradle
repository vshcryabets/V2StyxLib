buildscript {
  repositories {
    mavenCentral()
  }
}

allprojects {
  group = 'com.v2soft'
  version = '1.1.0'

  repositories {
    mavenCentral()
  }

  tasks.withType(Compile) {
    options.encoding = "UTF-8"
  }
}
