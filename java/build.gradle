buildscript {
  repositories {
    mavenCentral()
  }
}

allprojects {
  group = 'com.v2soft.styxlib'
  version = '1.0.0'

  repositories {
    mavenCentral()
  }

  tasks.withType(Compile) {
    options.encoding = "UTF-8"
  }
}
